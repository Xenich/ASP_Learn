@model ASPNET_Core_MVC_MODELS.Models.User

@{
    ViewData["Title"] = "CreateUser";
}

<h2>CreateUser</h2>

<h4>User</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateUser">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                                    @*<select asp-for="Name" class="form-control" asp-items="ViewBag.Names">
                                    <option value="">-- Задайте имя --</option>                                     // альтернативный вариант выбора имени из списка
                                    </select>*@
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Age" class="control-label"></label>
                <select asp-for="Age" class="form-control" asp-items="ViewBag.Ages">        @* Возраст будем выбирать из списка, который задали в контроллере *@
                    <option value="">-- Задайте возраст --</option>
                </select>
                @*<input asp-for="Age" class="form-control" />*@
                <span asp-validation-for="Age" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
